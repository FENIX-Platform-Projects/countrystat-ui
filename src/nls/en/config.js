/*global define*/
define(function () {

    'use strict';

    return {
        "freeText.footer" : "Type the exact keyword or if you wish to make a general research write the word with \"*\" at the end (e.g., \"pop*\" to find all the words that start with \“pop\”; e.g. \“animal*\” to find both singular and plural words)"
    }
});